
    
<section id="contact" class="py-20 relative overflow-hidden min-h-screen flex items-center">
  <!-- Fondo idéntico al diseño anterior -->
  <div class="hero-background absolute inset-0 z-0"></div>
  <div class="grid-pattern z-0"></div>
  
  <!-- Formas flotantes idénticas al diseño anterior -->
  <div class="floating-shapes absolute inset-0 z-0">
    <div class="floating-shape w-72 h-72 md:w-96 md:h-96 top-10 left-5 bg-blue-500/30 animation-delay-0"></div>
    <div class="floating-shape w-48 h-48 md:w-64 md:h-64 top-60 right-5 bg-purple-500/30 animation-delay-5000 animation-duration-20s"></div>
    <div class="floating-shape w-60 h-60 md:w-80 md:h-80 bottom-10 left-15 bg-cyan-500/30 animation-delay-10000 animation-duration-25s"></div>
  </div>
  
  <!-- Partículas -->
  <div class="particles absolute inset-0 z-0" id="particles-container"></div>
  
  <div class="container mx-auto px-4 relative z-10">
    <h2 class="text-5xl font-bold text-center mb-4 bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 bg-clip-text text-transparent bg-size-200 animate-textShine">
      Contáctame
    </h2>
    
    <p class="text-xl text-center text-gray-300 mb-12 max-w-2xl mx-auto">
      ¿Tienes un proyecto en mente? Hablemos sobre cómo puedo ayudarte a hacerlo realidad.
    </p>
    
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
      <!-- Disponibilidad -->
      <div class="lg:col-span-1 space-y-6">
        <div class="contact-info-card cyber-border rounded-xl p-6 hover:transform hover:-translate-y-2 transition-all duration-300">
          <h3 class="text-2xl font-bold mb-4 text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">
            Disponibilidad
          </h3>
          
          <div class="space-y-3">
            <div class="flex justify-between">
              <span class="text-gray-300">Proyectos Freelance</span>
              <span class="text-cyan-400 font-semibold">Disponible</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-300">Tiempo de Respuesta</span>
              <span class="text-cyan-400 font-semibold">24-48 horas</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-300">Idiomas</span>
              <span class="text-cyan-400 font-semibold">Español, Inglés</span>
            </div>
          </div>
          
          <div class="mt-4 pt-4 border-t border-gray-700">
            <div class="flex items-center">
              <div class="w-3 h-3 rounded-full bg-green-500 mr-2 animate-pulse"></div>
              <span class="text-green-400 font-medium">Disponible para nuevos proyectos</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Formulario de contacto -->
      <div class="lg:col-span-2">
        <div class="contact-card cyber-border rounded-xl p-8 hover:transform hover:-translate-y-2 transition-all duration-300">
          <h3 class="text-2xl font-bold mb-6 text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">
            Envíame un Mensaje
          </h3>
          
          <form 
            action="https://formspree.io/f/mqawdqyn"
            method="POST"
            class="space-y-6"
          >
            <!-- Campo anti-spam (Formspree lo maneja automáticamente) -->

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="name" class="block text-sm font-medium text-gray-400 mb-2">Nombre Completo</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  required
                  class="w-full px-4 py-3 contact-input rounded-lg focus:outline-none transition-all duration-300"
                  placeholder="Tu nombre"
                />
              </div>
              
              <div>
                <label for="email" class="block text-sm font-medium text-gray-400 mb-2">Correo Electrónico</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  required
                  class="w-full px-4 py-3 contact-input rounded-lg focus:outline-none transition-all duration-300"
                  placeholder="tu@email.com"
                />
              </div>
            </div>
            
            <div>
              <label for="subject" class="block text-sm font-medium text-gray-400 mb-2">Asunto</label>
              <input
                type="text"
                id="subject"
                name="subject"
                required
                class="w-full px-4 py-3 contact-input rounded-lg focus:outline-none transition-all duration-300"
                placeholder="¿Sobre qué quieres hablar?"
              />
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-gray-400 mb-2">Mensaje</label>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                class="w-full px-4 py-3 contact-input rounded-lg focus:outline-none transition-all duration-300"
                placeholder="Describe tu proyecto o consulta..."
              ></textarea>
            </div>
            
            <div class="flex items-center">
              <input
                type="checkbox"
                id="consent"
                name="consent"
                required
                class="w-4 h-4 text-cyan-500 bg-gray-800 border-gray-700 rounded focus:ring-cyan-500 focus:ring-2"
              />
              <label for="consent" class="ml-2 text-sm text-gray-400">
                Acepto que mis datos sean procesados según la 
                <a href="#" class="text-cyan-400 hover:underline">política de privacidad</a>
              </label>
            </div>
            
            <button
              type="submit"
              class="w-full bg-gradient-to-r from-cyan-500 to-blue-500 text-white py-3 px-6 rounded-lg hover:from-cyan-600 hover:to-blue-600 transition-all duration-300 shadow-lg hover:shadow-cyan-500/50 font-medium flex items-center justify-center"
            >
              <i class="fas fa-paper-plane mr-2"></i> Enviar Mensaje
            </button>
          </form>
        </div>
      </div>
    </div>
    
    <!-- Mensaje adicional -->
    <div class="mt-12 text-center max-w-3xl mx-auto">
      <div class="bg-gray-800/50 rounded-xl p-6 border border-gray-700/50">
        <h3 class="text-xl font-bold mb-3 text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">
          ¿Prefieres contactarme directamente?
        </h3>
        <p class="text-gray-300">
          También puedes escribirme directamente a través del formulario o contactarme a través de mis redes sociales. Estaré encantado de responder a tus preguntas.
        </p>
      </div>
    </div>
  </div>
</section>
<script>
// Generador de partículas y animación de tarjetas al hacer scroll
document.addEventListener('DOMContentLoaded', function () {
  const particlesContainer = document.getElementById('particles-container');

  if (particlesContainer) {
    const particleCount = 40;

    for (let i = 0; i < particleCount; i++) {
      const particle = document.createElement('div');
      particle.classList.add('particle');

      const left = Math.random() * 100;
      const delay = Math.random() * 15;
      const duration = 15 + Math.random() * 10;
      const size = 2 + Math.random() * 3;

      particle.style.left = `${left}%`;
      particle.style.animationDelay = `${delay}s`;
      particle.style.animationDuration = `${duration}s`;
      particle.style.width = `${size}px`;
      particle.style.height = `${size}px`;

      const colors = [
        'rgba(59, 130, 246, 0.7)',
        'rgba(139, 92, 246, 0.7)',
        'rgba(6, 182, 212, 0.7)'
      ];
      const randomColor = colors[Math.floor(Math.random() * colors.length)];
      particle.style.backgroundColor = randomColor;

      particlesContainer.appendChild(particle);
    }
  }

  // Animación de las tarjetas al hacer scroll
  const contactCards = document.querySelectorAll('.contact-card, .contact-info-card');
  const observerOptions = {
    threshold: 0.3,
    rootMargin: '0px 0px -100px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting && entry.target instanceof HTMLElement) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
      }
    });
  }, observerOptions);

  contactCards.forEach(card => {
    if (card instanceof HTMLElement) {
      card.style.opacity = '0';
      card.style.transform = 'translateY(20px)';
      card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
      observer.observe(card);
    }
  });

  // Netlify Forms maneja el envío automáticamente
});
</script>